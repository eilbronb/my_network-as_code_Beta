---
  - name: Gather Information
    hosts: us33-core-switch
    connection: local
    gather_facts: no

    tasks:
      - name: Gather IOS Facts
        ios_facts:
        register: output0

      - name: Print Facts
        debug:
          msg: "{{ output0 }}"
