---
  - name: Verify Connectivity
    hosts: us33
    connection: network_cli
    gather_facts: no

    tasks:
      - name: Ping Verification
        ios_command:
          commands:
            - show run
        register: ping_output
        debug:
          msg: "{{ ping_output }}"


