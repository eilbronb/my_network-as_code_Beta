---
  - name: Generate IOS Router Configurations
    connection: local
    hosts: scs
    gather_facts: no
    tasks:
      - template:
          src=templates/scs-templates.j2.j2
          dest=configs/{{ inventory_hostname }}
