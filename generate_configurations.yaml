---
  - name: Generate IOS Router Configurations
    connection: local
    hosts: SCS
    gather_facts: no
    tasks:
      - template:
          src=templates/scs-templates.j2
          #dest=configs/{{ inventory_hostname }}
          dest=host_vars/{{ inventory_hostname }}
